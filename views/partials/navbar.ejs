<!-- Navigation Bar -->
<header>
  <nav class="bg-blue-900">
    <div class="container mx-auto px-4 flex items-center justify-between h-16">
      <!-- Logo -->
      <div class="text-white text-2xl font-bold">
        <a href="/">Pasaana</a>
      </div>
      <!-- Navigation Links -->
      <div class="hidden md:flex items-center space-x-4">
        <!-- Buy A Home Dropdown Structure -->
        <div class="relative inline-block">
          <button id="buyDropdownButton" class="text-white hover:text-gray-200 flex items-center focus:outline-none" aria-haspopup="true" aria-expanded="false">
            Find Your Home
            <!-- Dropdown Icon -->
            <svg class="w-4 h-4 ml-1" fill="currentColor" viewBox="0 0 20 20">
              <path d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414L10 13.414 5.293 8.707a1 1 0 010-1.414z" />
            </svg>
          </button>
          <!-- Dropdown Menu -->
          <div id="buyDropdownMenu" class="dropdown-menu absolute left-0 mt-2 w-56 bg-blue-900 text-white rounded-md shadow-lg hidden z-10">
            <a href="/realtor-service" class="block px-4 py-2 hover:bg-blue-800">Learn About Our Realtor Services</a>
            <a href="/explorer" class="block px-4 py-2 hover:bg-blue-800">Explore Homes</a>
          </div>
        </div>
        <!-- Home Loans Dropdown Structure -->
        <div class="relative inline-block">
          <button id="productsDropdownButton" class="text-white hover:text-gray-200 flex items-center focus:outline-none" aria-haspopup="true" aria-expanded="false">
            Home Loans
            <!-- Dropdown Icon -->
            <svg class="w-4 h-4 ml-1" fill="currentColor" viewBox="0 0 20 20">
              <path d="M5.293 7.293a1 1 0 011.414 0L10 11.586l3.293-4.293a1 1 0 111.414 1.414l-4 5a1 1 0 01-1.414 0l-4-5a1 1 0 010-1.414z" />
            </svg>
          </button>
          <!-- Dropdown Menu -->
          <div id="productsDropdownMenu" class="dropdown-menu absolute left-0 mt-2 w-48 bg-blue-900 text-white rounded-md shadow-lg hidden z-10">
            <a href="/mortgage-loans" class="block px-4 py-2 hover:bg-blue-800">Mortgage Loan Options</a>
          </div>
        </div>
        <!-- Calculators Dropdown Structure -->
        <div class="relative inline-block">
          <button id="mortgageDropdownButton" class="text-white hover:text-gray-200 flex items-center focus:outline-none" aria-haspopup="true" aria-expanded="false">
            Calculators
            <!-- Dropdown Icon -->
            <svg class="w-4 h-4 ml-1" fill="currentColor" viewBox="0 0 20 20">
              <path d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414L10 13.414 5.293 8.707a1 1 0 010-1.414z" />
            </svg>
          </button>
          <!-- Dropdown Menu -->
          <div id="mortgageDropdownMenu" class="dropdown-menu absolute left-0 mt-2 w-48 bg-blue-900 text-white rounded-md shadow-lg hidden z-10">
            <a href="/calculators" class="block px-4 py-2 hover:bg-blue-800">Try Our Housing Calculators</a>
          </div>
        </div>
        <!-- Advising Dropdown Structure -->
        <div class="relative inline-block">
          <button id="advisingDropdownButton" class="text-white hover:text-gray-200 flex items-center focus:outline-none" aria-haspopup="true" aria-expanded="false">
            Advising
            <!-- Dropdown Icon -->
            <svg class="w-4 h-4 ml-1" fill="currentColor" viewBox="0 0 20 20">
              <path d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414L10 13.414 5.293 8.707a1 1 0 010-1.414z" />
            </svg>
          </button>
          <!-- Dropdown Menu -->
          <div id="advisingDropdownMenu" class="dropdown-menu absolute left-0 mt-2 w-64 bg-blue-900 text-white rounded-md shadow-lg hidden z-10">
            <a href="/home-loans-advisor" class="block px-4 py-2 hover:bg-blue-800">Talk to a Home Loans Advisor</a>
            <a href="/realtor-advisor" class="block px-4 py-2 hover:bg-blue-800">Talk to a Realtor Advisor</a>
          </div>
        </div>
        <!-- Additional Links (if any) -->
      </div>
      <!-- Authentication Buttons -->
      <div class="hidden md:flex items-center space-x-4">
        <% if (user) { %>
          <a href="/profile" class="bg-orange-500 text-white px-4 py-2 rounded-md hover:bg-orange-600 transition">Profile</a>
          <a href="/logout" class="text-white hover:text-gray-200">Sign Out</a>
        <% } else { %>
          <a href="/login?from=navbar" class="bg-orange-500 text-white px-4 py-2 rounded-md hover:bg-orange-600 transition">Sign In</a>
        <% } %>
      </div>
      <!-- Mobile Menu Button -->
      <div class="md:hidden">
        <button id="mobile-menu-button" class="text-white focus:outline-none">
          <svg class="w-6 h-6" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden">
      <ul class="bg-blue-900 text-white">
        <li><a href="/realtor-service" class="block px-4 py-2 hover:bg-blue-800">Learn About Our Realtor Services</a></li>
        <li><a href="/explore-homes" class="block px-4 py-2 hover:bg-blue-800">Explore Homes</a></li>
        <li><a href="/mortgage-loans" class="block px-4 py-2 hover:bg-blue-800">Mortgage Loans</a></li>
        <li><a href="/preapproval" class="block px-4 py-2 hover:bg-blue-800">Apply Now</a></li>
        <li><a href="/calculators" class="block px-4 py-2 hover:bg-blue-800">Try Our Housing Calculators</a></li>
        <li><a href="/talk-to-advisor" class="block px-4 py-2 hover:bg-blue-800">Talk to an Advisor</a></li>
        <% if (user) { %>
          <li><a href="/profile" class="block px-4 py-2 hover:bg-blue-800">Profile</a></li>
          <li><a href="/logout" class="block px-4 py-2 hover:bg-blue-800">Sign Out</a></li>
        <% } else { %>
          <li><a href="/login?from=navbar" class="block px-4 py-2 hover:bg-blue-800">Sign In</a></li>
        <% } %>
      </ul>
    </div>
  </nav>
</header>

<!-- Existing Script for Mobile Menu and Dropdowns -->
<script>
  // Mobile Menu Script
  const btn = document.getElementById('mobile-menu-button');
  const menu = document.getElementById('mobile-menu');

  btn.addEventListener('click', () => {
    menu.classList.toggle('hidden');
  });

  // Dropdown Menus Script
  const dropdownButtons = document.querySelectorAll("[id$='DropdownButton']");
  dropdownButtons.forEach((button) => {
    const menuId = button.id.replace('Button', 'Menu');
    const menu = document.getElementById(menuId);

    button.addEventListener('click', (event) => {
      event.preventDefault();
      const isHidden = menu.classList.contains('hidden');

      // Close other open dropdowns
      document.querySelectorAll('.dropdown-menu').forEach((dropdown) => {
        if (dropdown !== menu) {
          dropdown.classList.add('hidden');
        }
      });

      menu.classList.toggle('hidden');
      button.setAttribute('aria-expanded', !isHidden);
    });
  });

  // Close dropdowns when clicking outside
  window.addEventListener('click', (event) => {
    dropdownButtons.forEach((button) => {
      const menuId = button.id.replace('Button', 'Menu');
      const menu = document.getElementById(menuId);
      if (!button.contains(event.target) && !menu.contains(event.target)) {
        menu.classList.add('hidden');
        button.setAttribute('aria-expanded', false);
      }
    });
  });

  // Close dropdowns when pressing the Escape key
  window.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
      dropdownButtons.forEach((button) => {
        const menuId = button.id.replace('Button', 'Menu');
        const menu = document.getElementById(menuId);
        menu.classList.add('hidden');
        button.setAttribute('aria-expanded', false);
      });
    }
  });
</script>
