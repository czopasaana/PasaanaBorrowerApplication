<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pasaana - Sign In</title>
  <meta name="description" content="Sign in to your Pasaana account to access your dashboard and manage your preferences.">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Include Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Link to Custom CSS -->
  <link rel="stylesheet" href="/styles.css">
   <!-- Include jQuery for AJAX requests -->
   <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body class="bg-gray-100">
  <!-- Navigation Bar -->
  <%- include('partials/navbar.ejs') %>

  <!-- Sign In Form -->
  <main class="flex items-center justify-center py-12">
    <div class="bg-white rounded-lg shadow-lg p-8 max-w-md w-full">
      <h2 class="text-3xl font-bold text-blue-900 mb-6 text-center">Sign In to Your Account</h2>

      <% if (showMessage) { %>
        <p class="text-center text-gray-700 mb-4">You need to sign in to start the pre-approval process.</p>
      <% } %>

      <form action="/login" method="POST" class="space-y-6">
        <div>
          <label for="email" class="block text-gray-700 font-semibold">Email Address</label>
          <input type="email" name="email" id="email" required class="w-full mt-2 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
          <label for="password" class="block text-gray-700 font-semibold">Password</label>
          <input type="password" name="password" id="password" required class="w-full mt-2 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <button type="submit" class="w-full bg-blue-900 text-white px-4 py-2 rounded-md font-semibold text-lg hover:bg-blue-800 transition duration-300">Sign In</button>
      </form>
      <!-- Forgot Password Link -->
      <p class="text-center text-gray-700 mt-4">
        <a href="/forgot-password" class="text-blue-600 hover:underline">Forgot your password?</a>
      </p>
      <!-- Sign Up Link -->
      <p class="text-center text-gray-700 mt-6">
        Don't have an account? <a href="/signup?from=navbar" class="text-blue-600 hover:underline">Sign up here</a>
      </p>
    </div>
  </main>

  <!-- Chatbot -->
  <%- include('partials/chatbot.ejs') %>

  <!-- Footer -->
  <footer class="bg-blue-900 text-white text-center py-4 mt-12">
    <p>© 2024 Pasaana. All rights reserved.</p>
  </footer>
</body>
</html>